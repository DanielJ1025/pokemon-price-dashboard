services:
  - type: web
    name: pokemon-price-dashboard
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120 --workers 1
    healthCheckPath: /healthz
    autoDeploy: true
